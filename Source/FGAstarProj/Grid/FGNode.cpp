#include "FGNode.h"

AFGNode::AFGNode()
{
	
}

void AFGNode::SetNeighbours(int Width, int Height, TArray<AFGNode> NodesList)
{
	//TODO FLytta till FGGrid
	// AllDirections.Add(FVector(1,1,0));
	// AllDirections.Add(FVector(1,-1,0));
	// AllDirections.Add(FVector(-1,-1,0));
	// AllDirections.Add(FVector(-1,1,0));
	// AllDirections.Add(FVector(0,1,0));
	// AllDirections.Add(FVector(1,0,0));
	// AllDirections.Add(FVector(0,-1,0));
	// AllDirections.Add(FVector(-1,0,0));
	//
	// for(int i = 0; i < AllDirections.Num(); i++)
	// {
	// 	int NewX = XIndex + AllDirections[i].X;
	// 	int NewY = YIndex + AllDirections[i].Y;
	//
	// 	bool IsWithinBounds = NewX >= 0 && NewX < Width && NewY >= 0 && NewY < Height;
	// 	if(IsWithinBounds && NodesList[(NewY * Width) + NewX] != nullptr)
	// 	{
	// 		
	// 	}
	// }
	
}
